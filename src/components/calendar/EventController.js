import { fetch, post } from "../../util/SpringBootAdapter";

const EventController = {
    
    fetchEvents: () => {
        //fetch("/")
    },
    postEvent: (event) => {
        //post
    }
}

export default EventController;